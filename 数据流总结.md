# s1mpleFps 项目数据流全景总结

## 一、输入流 (Enhanced Input → 角色动作)

```
物理按键
  ↓
UInputAction (蓝图配置的 Input Action 资产)
  ↓
UEnhancedInputComponent::BindAction
  ↓
As1mpleFpsCharacter / As1mpleFpsPlayerController 的回调函数
```

### PlayerController 层（UI/全局输入）
| 按键 | InputAction | 回调 | 效果 |
|------|-----------|------|------|
| Tab | ScoreboardAction | `ToggleScoreboard()` | 开关计分板 |
| B | BuyMenuAction | `ToggleBuyMenu()` | 开关购买菜单 |
| Y/U | OpenTeamChat/OpenAllChat | `OpenChatBox(bool)` | 打开队聊/全部聊天 |
| Esc | EscapeAction | `OnEscapePressed()` | 暂停/关闭聊天框 |

### Character 层（角色动作输入）
| 按键 | InputAction | 回调 | 效果 |
|------|-----------|------|------|
| WASD | MoveAction | `Move()` | 移动 + 脚步声噪音 |
| 鼠标 | LookAction | `Look()` | 视角旋转 |
| 空格 | JumpAction | `Jump/StopJumping` | 跳跃 |
| E | Interaction | `Interact()` | 拾取武器 |
| R | ReloadAction | `Reload()` | 装弹 |
| Ctrl | CrouchAction | `StartCrouch/EndCrouch` | 蹲伏 |
| V | ToggleViewAction | `ToggleView()` | 切换第一/第三人称 |
| Esc | PauseAction | `PauseGame()` | 暂停 |
| 1/2/3 | WeaponSlot1/2/3Action | `OnWeaponSlot1/2/3()` | 切到武器槽1/2/3 |
| 滚轮 | NextWeaponAction/PrevWeaponAction | `NextWeapon/PreviousWeapon()` | 下一把/上一把武器 |
| H | UseHealthAction | `OnUseHealth()` | 使用医疗物品 |

### 武器层输入（UTP_WeaponComponent::Equip 时动态绑定）
| 按键 | InputAction | 回调 | 效果 |
|------|-----------|------|------|
| 鼠标左键 | WeaponFireAction | `StartFire/StopFire()` | 开火/停止开火 |
| R | WeaponReloadAction | `Reload()` | 武器装弹 |
| V | WeaponSwitchAction | `SwitchFireMode()` | 切换射击模式 |
| 鼠标右键 | WeaponAimAction | `ToggleAiming()` | ADS 瞄准 |

---

## 二、射击/战斗流 (Fire → Projectile → Damage → Death)

```
StartFire() → StartSingleFire()
  ├── 客户端: 乐观扣弹 + 本地特效 + ServerFireWeapon RPC → ServerFire → PerformFire + MulticastFireEffect
  └── 服务端: 直接扣弹 + PerformFire + MulticastFireEffect
  
PerformFire()
  → SpawnActor<As1mpleFpsProjectile> (设置 Damage, ArmorPenetration)
    → 子弹飞行 (ProjectileMovement)
      → OnHit()
        → DamageComponent::ApplyDamage(HitBone, Damage, Penetration, Instigator, HitLocation)
          → 伤害计算（见第三节）
          → OnDamaged.Broadcast(ActualDamage, Instigator)
            → 角色: OnHealthDamaged() → ReplicatedHealth 更新 → OnHealthChanged.Broadcast → HUD 更新
            → 敌人: OnDamaged() → 播放受击动画 + bIsStaggered
          → UAISense_Damage::ReportDamageEvent（AI 感知系统）
          → 如果死亡: OnDeath.Broadcast()
            → 角色: Die() → bIsDead=true → DisableMovement → ShowDeathScreen → RespawnTimer
            → 敌人: die() → StopAI → DisableCollision → PlayDeathAnim → SetLifeSpan
        → PlayHitMarker (击中敌人红色 / 击中墙壁白色)
        → PlaySound (HitBodySound / HitWallSound)
```

---

## 三、伤害计算流 (DamageComponent::ApplyDamage)

```
BaseDamage (武器基础伤害)
  × HitZone 倍率 (UWeaponDataAsset, 按骨骼名称查表，默认 1.0)
  = ZoneDamage
  × (1 - max(0, 护甲减伤 - 穿透值))
  = FinalDamage
  → 实际扣血 = min(FinalDamage, CurrentHealth)（不能扣超过剩余血量）
```

### 护甲减伤查表
```
UDamageComponent::GetReductionForBones(HitBone)
  → 遍历所有 EquippedArmors
    → UArmorData::GetReduction(HitBone)
      → 遍历 Armor 里的 FNameToReduction 数组
        → 检查 CoveredBones 是否包含 HitBone
          → 是: 返回 DamageReduction
          → 否: 返回默认 Reduction
  → 返回所有护甲中的最高减伤值
```

---

## 四、网络复制流 (Replication & RPC)

### 复制属性 (Replicated Properties)

| 类 | 属性 | ReplicatedUsing | 客户端效果 |
|----|------|-----------------|-----------|
| **As1mpleFpsCharacter** | ReplicatedHealth | OnRep_Health | 同步 DamageComponent->CurrentHealth + 广播 OnHealthChanged + HUD 更新 |
| | ReplicatedMaxHealth | OnRep_MaxHealth | 同步 DamageComponent->MaxHealth（客户端血量上限用权威值，不再依赖本地默认值） |
| | bIsDeadReplicated | OnRep_bIsDead | true→Die(), false→RespawnVisuals() |
| | CurrentWeapon | OnRep_CurrentWeapon | UnEquip 旧武器 + Equip 新武器 + HUD 绑定 |
| | WeaponInventory | OnRep_WeaponInventory | 远程角色隐藏所有武器; 本地处理挂起购买 |
| | WeaponIndex | - | 配合 WeaponInventory 使用 |
| | HealthTypes | OnRep_HealthItems | 广播 OnHealthItemsChanged |
| | HealthAmount | OnRep_HealthItems | 同上 |
| **As1mpleFpsPlayerState** | Kills | OnRep_Kills | 广播 OnScoreChanged |
| | Deaths | OnRep_Deaths | 广播 OnScoreChanged |
| | Scores | OnRep_Scores | 广播 OnScoreChanged |
| | Money | OnRep_Money | 广播 OnMoneyChanged |
| | KillStreak | - | 连杀数（经济奖励用） |
| | DeathStreak | - | 连死数（经济补偿用） |
| | TeamId | - | 队聊过滤用 |
| **As1mpleFpsGameState** | MatchTimeRemaining | OnRep_MatchTimeRemaining | 广播 OnMatchTimeChanged → HUD 更新倒计时 |
| | bMatchStarted | - | 比赛是否开始 |
| | bMatchEnded | OnRep_MatchEnded | 广播 OnMatchEnded(WinnerName, bWinByKill) |
| | WinnerName | OnRep_MatchEnded | 同上 |
| | bWinByKill | OnRep_MatchEnded | 同上 |
| | bSuddenDeath | OnRep_SuddenDeath | 广播 OnSuddenDeath → HUD 时间变红 |
| | OvertimeRemaining | OnRep_OvertimeChanged | 广播 OnOvertimeChanged → HUD 显示加时倒计时+滴答声 |
| | bIsWarmUp | - | 热身阶段标志 |
| | WarmUpTime | OnRep_WarmUpRemaining | 广播 OnWarmUpTimeChanged → HUD 倒计时大字 |
| **UTP_WeaponComponent** | ReplicatedCurrentAmmo | OnRep_CurrentAmmo | CurrentAmmo = 服务器值, 广播 OnAmmoChanged |
| | ReplicatedSpareAmmo | OnRep_SpareAmmo | SpareAmmo = 服务器值, 广播 OnAmmoChanged |
| | WeaponData | OnRep_WeaponData | 补充网格/音效/输入配置 + 更新 HUD 装备名 |

### RPC 调用流

| RPC | 方向 | 调用者 | 效果 |
|-----|------|--------|------|
| **ServerFireWeapon** | Client→Server | 客户端 StartSingleFire | 服务端 ServerFire → PerformFire + MulticastFireEffect |
| **ServerReloadWeapon** | Client→Server | 客户端 Reload | 服务端立即补弹 |
| **ServerSwitchWeapon** | Client→Server | 切枪槽 1/2/3 | 服务端 SwitchWeapon |
| **ServerNextWeapon** | Client→Server | 滚轮下 | 服务端 NextWeapon |
| **ServerPreviousWeapon** | Client→Server | 滚轮上 | 服务端 PreviousWeapon |
| **ServerPickUpWeapon** | Client→Server | Interact | 服务端验证+AttachWeapon+MulticastOnPickUp |
| **ServerRequestRespawn** | Client→Server | UI按钮 | 服务端 Respawn() |
| **ClientOnRespawn** | Server→Client | 服务端 Respawn() | 可靠推送权威血量(CurrentHealth+MaxHealth)刷新客户端血条，不依赖 OnRep 顺序 |
| **ServerPurchaseItem** | Client→Server | 购买菜单 | 服务端验证金钱+GrantWeapon/GrantArmor |
| **ServerUseHealth** | Client→Server | 使用医疗物品 | 服务端 UseHealth |
| **ServerReceivedMessage** | Client→Server | 聊天提交 | 服务端转发 MulticastReceivedChatMessage |
| **ClientUndoPickUp** | Server→Client | 服务端拒绝拾取 | 客户端回滚武器库存 |
| **ClientSyncWeaponAmmo** | Server→Client | 服务端切枪后 | 同步指定武器的弹药数 |
| **ClientPurchaseComplete** | Server→Client | 购买成功 | 客户端切换到新武器槽 |
| **ClientGrantArmorComplete** | Server→Client | 购买护甲成功 | 客户端添加护甲 + HUD 更新 |
| **MulticastOnPickUp** | Server→All | 拾取成功 | 其他客户端隐藏武器+标记已拾取 |
| **MulticastFireEffect** | Server→All | 开火 | 非本地玩家播放枪口火焰/声音/动画 |
| **MulticastFireEffects** | Server→All | AI 开火 | 所有客户端播放 AI 开火特效/声音/动画 |
| **MulticastKillPlay** | Server→All | GameMode OnKill | 所有客户端 OnKillPlay.Broadcast → HUD 击杀播报 |
| **MulticastReceivedChatMessage** | Server→All | 聊天消息 | 所有客户端 OnMessageReceived.Broadcast → HUD 聊天显示 |

### 客户端预测策略
- **切枪**: 客户端先本地切换 + 发 RPC（乐观预测），服务器拒绝无回滚（切枪不会失败）
- **拾取武器**: 客户端先本地拾取 + 发 RPC；服务器拒绝时 ClientUndoPickUp 回滚
- **购买武器**: 客户端发 ServerPurchaseItem；服务器创建武器后 ClientPurchaseComplete；武器组件动态创建可能晚于 RPC 到达，有 OnRep_WeaponInventory 重试机制（最多 10 次，间隔 0.1s）
- **开火**: 客户端发 ServerFireWeapon（含武器索引+位置）；服务器用索引匹配武器（兼容切枪延迟）
- **使用医疗物品**: 客户端乐观开始治疗 CD + 发 RPC；服务器独立执行

---

## 五、AI 感知与行为流

```
世界事件（噪音/视觉/伤害）
  ↓
UAIPerceptionComponent 接收刺激
  ├── UAISense_Sight: 玩家在视野内 → OnPerceptionUpdated
  ├── UAISense_Hearing: 脚步声 (0.3f) / 枪声 (1.0f) → OnPerceptionUpdated
  └── UAISense_Damage: 被击中 → OnPerceptionUpdated

OnPerceptionUpdated() → 评估所有感知到的 Actor
  ├── 过滤: 跳过自身和友军（其他 AEnemyCharacter），防止小队自相残杀
  ├── 打分: Seen=1000, Damaged=500, Heard=100, -距离*0.1
  ├── 设置 Blackboard: TargetActor, TargetLocation, HasLineOfSight, LastSeenLocation, LastHeardLocation
  └── SetFocus(BestTarget) → AAIController 自动旋转朝向目标

Tick() 每帧:
  ├── UpdateAlertValue(DeltaTime)
  │   ├── 最近有感 → AlertValue += 增量
  │   ├── 最近无感 → AlertValue -= DecaySpeed * DeltaTime
  │   └── AlertValue 映射到 AlertLevel (Calm/Suspicious/Alert/Combat)
  │       └── AlertLevel 映射到 AIState (Idle/Investigate/Combat)
  │           └── 血量过低 或 弹尽 → Retreat 覆盖
  │
  ├── UpdateBlackboardState() — 同步 CurrentAmmo, Health, HasLineOfSight, OptimalDistance 到 Blackboard
  │
  └── ShareTargetWithSquad() — 如果自己是战斗状态+有目标，强制写入小队成员的 BB

EQS (Environment Query System):
  ├── EQC_TargetActor — 查询最近的目标 Actor
  └── EQC_TargetLocation — 查询最佳战斗/掩护位置

Behavior Tree 节点:
  ├── BTService_UpdateAlertValue — 服务: 更新 AlertValue
  ├── BTService_UpdateBlackboard — 服务: 同步状态到 Blackboard
  ├── BTService_Fire — 服务: 战斗状态自动调用 EnemyFire()
  ├── BTDecorator_CheckAIState — 条件: 检查 AIState
  ├── BTDecorator_HasTarget — 条件: 有 TargetActor
  ├── BTDecorator_HasAmmo — 条件: 有弹药
  ├── BTTask_Fire — 任务: 调用 EnemyFire()
  ├── BTTask_Reload — 任务: AI 装弹
  ├── BTTask_Investigate — 任务: 调查最后已知位置
  ├── BTTask_FindCover — 任务: EQS 寻找掩体
  └── BTTask_Retreat — 任务: 撤退行为

EnemyFire() → AI 开火
  ├── 检查: 非硬直, TargetActor 存在且存活, 超过反应时间, 朝向目标, 有弹药
  ├── Burst 控制: BurstShots 发 / BurstPause 冷却
  ├── 散布计算: 基于 FireAccuracy + 距离的锥形散布
  ├── LineTraceSingleByObjectType (ECC_WorldStatic + ECC_Pawn)
  │   └── 命中友军（其他 AEnemyCharacter）→ 跳过伤害；否则 VictimDmg->ApplyDamage(HitBone, BaseDamage, Penetration, Enemy, HitLocation)
  ├── MulticastFireEffects (音效+动画 在所有客户端播放)
  └── ReportNoiseEvent("Gunshot") — 枪声触发其他 AI 感知

Squad 小队协调:
  ├── OnPossess 延迟 0.5s → FindSquadMembers() (按 SquadRadius 搜索附近 AI)
  ├── ApplyRoleModifiers() — 根据 Assault/Suppressor/Flanker 角色调整参数
  └── ShareTargetWithSquad() — 在 Tick 中向队友共享目标+强制拉满 AlertValue
      ├── SquadMembers 为 TWeakObjectPtr 弱引用，共享前 RemoveAll 清理已死亡队友
      └── 目标为友军时不共享（防止队友互瞄）
```

---

## 六、比赛状态流 (GameState 时间线)

```
GameMode::BeginPlay()
  → GameState::StartCountdown()
    → bIsWarmUp = true
    → WarmUpTime = GM->WarmUpDuration
    → 启动 WarmUpHandle (每 1s TickWarmUp)

WarmUp 阶段:
  ├── TickWarmUp(): WarmUpTime-- → OnWarmUpTimeChanged.Broadcast → HUD 倒计时
  ├── 角色 Move/Look/Die 被 bIsWarmUp 阻塞（热身结束前冻结）
  └── WarmUpTime <= 0:
      → bIsWarmUp = false
      → bMatchStarted = true
      → 启动 CountdownHandle (每 1s TickCountdown)

比赛阶段:
  ├── TickCountdown(): MatchTimeRemaining-- → OnMatchTimeChanged.Broadcast → HUD 更新
  └── MatchTimeRemaining <= 0:
      → OnMatchTimeUp()
        ├── 找最高击杀 → 唯一则 AnnounceWinner(name, false)
        └── 平局 → bSuddenDeath = true → StartOvertime()

加时赛 (Sudden Death):
  ├── StartOvertime():
  │   ├── 记录所有玩家当前击杀数 (OvertimeStartKills)
  │   ├── 启动 OvertimeHandle (硬性时限)
  │   └── 启动 OvertimeRemainingHandle (每 1s OvertimeCountDown)
  ├── 击杀时 CheckWinnerCondition():
  │   └── 加时赛击杀数 >= OvertimeKillTargets → AnnounceWinner(name, true)
  └── 时限到 OnOvertimeUp():
      └── tiebreaker 规则: Kills > Deaths > Scores → AnnounceWinner

比赛结束:
  AnnounceWinner(Winner, bWinByKill):
    ├── bMatchEnded = true, WinnerName, bWinByKill 复制到所有客户端
    ├── OnMatchEnded.Broadcast → HUD 显示 WBP_MatchEndPanel (胜利/失败)
    └── 清除所有 Timer (Countdown, Overtime, OvertimeRemaining)
```

---

## 七、击杀/分数/经济流

```
角色死亡 → Die() → (服务端) GM->OnKill(KillerPS, VictimPS)
  ↓
As1mpleFpsPvPGameMode::OnKill():
  ├── Killer: AddKill() + IncrementKillStreak() + AddMoney(CalculateKillReward)
  ├── Victim: AddDeath() + IncrementDeathStreak() (清理 KillStreak)
  ├── GameState::MulticastKillPlay(KillerName, VictimName) → 所有客户端击杀播报
  └── CheckWinnerCondition(Killer) → 击杀数 >= KillLimits → AnnounceWinner

经济系统 (CalculateKillReward):
  ├── KillRewardBase: 300 (蓝图可调)
  ├── + KillStreak × KillStreakBonus (100)    // 连杀加成
  └── + DeathStreak × DeathStreakBonusPerLevel (50)  // 连死补偿

PlayerState 更新:
  ├── AddKill(): Kills++, KillStreak++, Scores+=10
  │   └── OnRep_Kills → OnScoreChanged.Broadcast → HUD UpdateScoreDisplay
  ├── AddDeath(): Deaths++, KillStreak=0, DeathStreak++
  │   └── OnRep_Deaths → OnScoreChanged.Broadcast
  └── AddMoney(Amount): Money += Amount
      └── OnRep_Money → OnMoneyChanged.Broadcast → HUD 更新金钱
```

---

## 八、购买系统流 (Economy/Buy Menu)

```
热身阶段: bIsWarmUp = true
  ↓
玩家按 B → As1mpleFpsPlayerController::ToggleBuyMenu()
  ├── 创建/关闭 WBP_BuyMenu Widget
  └── FInputModeGameAndUI + bShowMouseCursor

购买菜单:
  ├── GetBuyItems() → 读取 UBuyEquipmentData 资产中的 Items 数组
  ├── GetCurrentMoney() → 从 PlayerState 获取当前金钱
  └── 玩家点击购买条目 → RequestPurchase(ItemIndex)
      ├── 客户端验证: 金钱 >= 价格 → ServerPurchaseItem RPC
      └── 服务端:
          ├── TrySpendingMoney(Price) → PS->Money -= Price
          ├── 武器: PC->GrantWeapon(WeaponClass, WeaponData)
          │   ├── NewObject<UTP_WeaponComponent> → RegisterComponent → AttachWeapon
          │   ├── 替换/添加武器槽位 → SwitchWeapon
          │   └── ClientPurchaseComplete(SlotIndex) → 客户端切换到新武器
          ├── 护甲: PC->GrantArmor(ArmorData)
          │   ├── DamageComponent->EquippedArmors.AddUnique(ArmorData)
          │   └── ClientGrantArmorComplete(ArmorData) → 客户端同步+HUD更新
          └── 医疗物品: PC->GrantHealthItem(HealthData)
              ├── HealthTypes.Add / HealthAmount++
              └── OnHealthItemsChanged.Broadcast

购买重试机制 (OnRep_WeaponInventory):
  ├── ClientPurchaseComplete RPC 可能在 WeaponInventory 复制之前到达
  ├── PendingPurchaseIndex >= 0 → 每次 OnRep 检查槽位是否有效
  └── 无效则 SetTimer 0.1s 重试，最多 10 次
```

---

## 九、聊天系统流

```
玩家按键 → As1mpleFpsPlayerController::OpenChatBox(bIsTeam)
  ├── 创建 UChatWidget (含 UEditableTextBox)
  ├── FInputModeGameAndUI + bShowMouseCursor
  └── bCurrentChatIsTeam = bIsTeam

玩家输入消息 → ChatWidget::OnChatTextCommittedHandler()
  → OnChatTextCommitted(Text, CommitMethod)
    → CommitMethod == OnEnter:
      → SentMessageToServer(Message, bCurrentChatIsTeam)
        → CloseChatBox()
        → ServerReceivedMessage RPC

服务器端 (ServerReceivedMessage_Implementation):
  ├── 获取发送者名称
  ├── 如果 bIsTeam (队聊):
  │   → 遍历所有 PlayerController
  │   → 筛选 TeamId 相同的玩家
  │   → 逐个 ClientReceivedMessage RPC
  └── 如果 !bIsTeam (全部聊天):
      → GS->MulticastReceivedChatMessage(Sender, Message, false)

客户端接收:
  └── GameState::OnMessageReceived.Broadcast
      → HUDWidget::OnChatMessageReceived(Sender, Message, bIsTeam)
        ├── 格式: [队聊] Sender: Message (蓝色) / Sender: Message (白色)
        ├── 添加到 ChatMessageBox (VerticalBox + UTextBlock)
        └── 自动删除: ChatMessageLifetime (10s) 后移除
```

---

## 十、武器库存流 (Weapon Inventory)

```
武器获取三种途径:
  ├── 1. 拾取 (Interact) — E 键捡地上的武器
  ├── 2. 购买 (Purchase) — 热身阶段 B 键购买
  └── 3. 初始装备 — BP_FirstPersonCharacter 默认携带

拾取流程:
  Interact()
    → OverlapMultiByChannel (围绕摄像机前方150cm的200cm半径球体)
    → 过滤: 无 PickUpComponent / 已拾取 / 超出距离 / 不在视角内
    → 客户端: ServerPickUpWeapon RPC + 乐观本地 AttachWeapon
    → 服务端: 验证 → AttachWeapon → WeaponInventory.Add → MulticastOnPickUp
    → 如果服务端拒绝 → ClientUndoPickUp 回滚本地库存

切枪流程:
  SwitchWeapon(Index):
    ├── 旧武器 UnEquip (ClearBindingsForObject, RemoveMappingContext)
    ├── 新武器 Equip (BindAction 输入, AddMappingContext, 同步弹药)
    ├── PreviousClientWeapon = CurrentWeapon
    ├── CurrentWeapon = NewWeapon
    ├── WeaponIndex = Index
    └── 本地玩家: HUDWidget->BindToWeapon(NewWeapon)

OnRep_CurrentWeapon (远程客户端收到复制):
  本地玩家: 补 Equip + HUD 绑定
  远程角色: UnEquip 旧 + Equip 新 (武器始终隐藏)

OnRep_WeaponInventory:
  本地玩家: 处理 PendingPurchaseIndex + 重试机制
  远程角色: 所有武器隐藏 + 同步 CurrentWeapon

最大武器槽: MaxWeaponSlots = 3
```

---

## 十一、治疗/医疗物品流

```
玩家按 H → OnUseHealth()
  → 检查 HealthTypes[0] 和 HealthAmount[0] > 0
  → UseHealth(0)

UseHealth(HealthIndex):
  客户端:
    ├── 乐观开始: bIsHealing=true, HealingDuration=Data->UsingTime
    ├── 减速: MaxWalkSpeed *= HealingSpeedMultiplier (0.5)
    ├── OnHealingStateChanged.Broadcast → HUD 显示治疗倒计时
    ├── SetTimer(HealingHandle, Data->UsingTime, false)
    │   └── 计时器到: 恢复速度, bIsHealing=false, HUD隐藏
    └── ServerUseHealth RPC

  服务端:
    ├── 同客户端: 开始减速 + 设置计时器
    └── 计时器到:
        ├── CurrentHealth += Data->HealAmount (不超过 MaxHealth)
        ├── ReplicatedHealth = CurrentHealth → 复制到客户端
        ├── HealthAmount[Index]-- → 消耗1个
        ├── 如果耗尽: 从 HealthTypes/HealthAmount 移除
        └── OnHealthItemsChanged.Broadcast

HealthTypes 和 HealthAmount 通过 OnRep_HealthItems 复制到客户端
```

---

## 十二、Session 流 (GameInstance)

```
主菜单 Map (StartMap):
  ├── HostGame(SessionName, MaxPlayers, bIsLAN)
  │   → SessionInterface->CreateSession()
  │     → OnCreateSessionComplete
  │       → OpenLevel(TravelMapName + "?listen")
  │
  ├── FindSession(MaxSearchResults, bIsLAN)
  │   → SessionInterface->FindSessions()
  │     → OnFindSessionCompleteInternal
  │       → OnFindSessionComplete.Broadcast (UI 刷新列表)
  │       → LastSearchResult = SearchResult
  │
  ├── JoinSession(SessionIndex)
  │   → SessionInterface->JoinSession(LastSearchResult, Index)
  │     → OnJoinSessionCompleteInternal
  │       → ClientTravel(ConnectionString)
  │       → OnJoinSessionComplete.Broadcast(bWasSuccessful)
  │
  └── LeaveSession()
      → SessionInterface->DestroySession()
        → 返回主菜单

GetSessionInfo(Index) — 获取搜索结果列表信息供 UI 显示
GetSessionCount() — 搜索结果总数
```

---

## 十三、UI/HUD 绑定流

```
As1mpleFpsPlayerController::BeginPlay()
  ├── 菜单地图 → 创建 MainMenuWidget (WBP_StartMap)
  └── 战斗地图 → 创建 HUDWidget (WBP_MainHUD)

HUDWidget::NativeConstruct():
  ├── 初始化默认值 (弹药=0, 血量=100, 计分=0, 时间=10:00)
  ├── 隐藏初始状态: HitMarker, Scoreboard, AmmoText, HealingText
  └── TryBindPlayerState()

TryBindPlayerState() — 每帧 NativeTick 重试，直到全部绑定成功:
  ├── PlayerState::OnScoreChanged → UpdateScoreDisplay(Kills, Deaths, Scores)
  ├── GameState::OnMatchTimeChanged → UpdateMatchTimeDisplay(TimeRemaining)
  ├── GameState::OnKillPlay → OnKillPlayReceived(Killer, Victim)
  ├── GameState::OnMatchEnded → OnMatchEndedReceived(Winner, bWinByKill)
  ├── GameState::OnSuddenDeath → OnSuddenDeathReceived()
  ├── GameState::OnOvertimeChanged → OnOvertimeReceived(OvertimeRemaining)
  ├── GameState::OnWarmUpTimeChanged → OnWarmUpReceived(WarmUpTime)
  └── GameState::OnMessageReceived → OnChatMessageReceived(Sender, Message, bIsTeam)

OnPossess → 武器/护甲/血量绑定:
  ├── Character::OnHealthChanged → OnPawnHealthChanged → HUD::UpdateHealthDisplay
  ├── Character::OnHealingStateChanged → OnPawnHealingStateChanged → HUD::UpdateHealingDisplay
  ├── CurrentWeapon → HUD::BindToWeapon (绑定 OnAmmoChanged → UpdateAmmoDisplay)
  └── 触发 OnRep_Health() 确保初始血量正确

HUD 刷新路径（多通道保证可靠性）:
  通道A: OnRep_Health → Direct HUD Update（最可靠，不用等委托绑定）
  通道B: OnRep_Health → Broadcast Delegate → PC::OnPawnHealthChanged → HUD::UpdateHealthDisplay
  通道C（复活专用）: 服务端 Respawn() → ClientOnRespawn(权威血量) → 客户端确定性拉满血条（不依赖 OnRep_Health/OnRep_bIsDead 的先后顺序）
  血量上限 MaxHealth 也复制（ReplicatedMaxHealth），客户端血条百分比不再依赖本地默认值
```

---

## 十四、第一/第三人称切换流

```
ToggleView():
  ├── 冷却: 0.25s 防快速连点
  ├── 退出 ADS (防止 FOV/弹簧臂状态冲突)
  ├── bIsThirdPerson = !bIsThirdPerson
  └── 切换到第三人称:
      ├── ThirdPersonCamera->SetActive(true)
      ├── FirstPersonCameraComponent->SetActive(false)
      ├── Mesh1P->SetVisibility(false)
      ├── GetMesh()->SetOwnerNoSee(false)  // 显示第三人称模型
      ├── ThirdPersonSpringArm->bUsePawnControlRotation = true
      └── ReattachWeaponsForView(true)
          └── 所有武器 SetVisibility(false) + SetHiddenInGame(true)
              （第三人称没有配套武器动画，全部隐藏）
  └── 切换到第一人称:
      ├── ThirdPersonCamera->SetActive(false)
      ├── FirstPersonCameraComponent->SetActive(true)
      ├── Mesh1P->SetVisibility(true)
      ├── GetMesh()->SetOwnerNoSee(true)  // 隐藏第三人称模型
      └── ReattachWeaponsForView(false)
          └── 所有武器 AttachToComponent(Mesh1P, GripPoint) + SetOnlyOwnerSee(true)
              + 仅当前武器可见
```

---

## 十五、AI 死亡与重生流 (PvE 单机模式)

```
AEnemyCharacter::die():
  ├── StopAI: StopMovement + BrainComponent→StopLogic + UnPossess + DestroyController
  ├── DisableCollision: Mesh + Capsule 碰撞关闭
  ├── HideHealthBar: HealthBarWidget->SetVisibility(false)
  ├── 播放 DeathMontage 或 Ragdoll 物理
  ├── SetLifeSpan(0.5f) — 0.5秒后自动销毁
  └── 通知 GameMode:
      ├── GM->OnKill(LastInstigator, this) — 统计击杀
      └── GM->ScheduleEnemyRespawn(GetClass(), OriginalLocation, OriginalRotation, 5.0s)
          → 5秒后在原位置重生一个新的敌人（GetWorld() 为空时跳过，防止重启/退出时崩溃）
```

---

## 十六、数据资产依赖图 (DataAssets)

```
UWeaponData (UPrimaryDataAsset)
  ├── 被 UTP_WeaponComponent::WeaponData 引用 (Replicated)
  ├── 被 AEnemyCharacter::EnemyWeapon 引用
  ├── 被 FBuyItemEntry::WeaponData 引用 (购买菜单)
  └── 包含: BaseDamage, MaxProjectile, FireRate, ReloadTime, FireMode,
             ArmorPenetration, Recoil, Spread, MuzzleShake, WeaponMesh,
             ProjectileClass, WeaponMappingContext, InputActions, Price

UWeaponDataAsset (UPrimaryDataAsset)
  ├── 被 UDamageComponent::HitZoneData 引用
  └── 包含: HitZones (FHitZoneEntry: BoneName → DamageMul)

UArmorData (UPrimaryDataAsset)
  ├── 被 UDamageComponent::EquippedArmors 引用
  ├── 被 FBuyItemEntry::ArmorData 引用
  └── 包含: Armor (FNameToReduction: ArmorName → DamageReduction + CoveredBones)

UHealthData (UPrimaryDataAsset)
  ├── 被 As1mpleFpsCharacter::HealthTypes 引用 (Replicated)
  ├── 被 FBuyItemEntry::HealthData 引用
  └── 包含: HealAmount, UsingTime, Price, MedicineName

UBuyEquipmentData (UPrimaryDataAsset)
  ├── 被 As1mpleFpsPlayerController::BuyEquipmentData 引用
  └── 包含: Items (FBuyItemEntry: Price, DisplayName, WeaponClass,
             WeaponData, ArmorData, HealthData)
```

---

## 十七、全局数据流总览图

```
┌─────────────────────────────────────────────────────────────────────┐
│                          US1mpleFpsGameInstance                     │
│  HostGame / FindSession / JoinSession / LeaveSession                │
│  (OnlineSubsystemSteam → Session 创建/搜索/加入)                      │
└─────────────────────────────────────────────────────────────────────┘
                                    ↓
┌─────────────────────────────────────────────────────────────────────┐
│                      As1mpleFpsPvPGameMode (服务端权威)               │
│  OnKill → Kill/Death/Score/Economy → CheckWinnerCondition           │
│  WarmUpDuration → GameState::StartCountdown                         │
└───────┬─────────────────────────────────────────────────────────────┘
        ↓ 驱动
┌─────────────────────────────────────────────────────────────────────┐
│                      As1mpleFpsGameState (Replicated)                │
│  MatchTimeRemaining, bIsWarmUp, bMatchStarted, bMatchEnded          │
│  bSuddenDeath, OvertimeRemaining, WarmUpTime, WinnerName            │
│  Delegates: OnMatchTimeChanged, OnKillPlay, OnMatchEnded,           │
│             OnSuddenDeath, OnOvertimeChanged, OnWarmUpTimeChanged   │
│  Multicast RPCs: MulticastKillPlay, MulticastReceivedChatMessage    │
└───────┬─────────────────────────────────────────────────────────────┘
        ↓ OnKillPlay / OnMessageReceived
┌─────────────────────────────────────────────────────────────────────┐
│          As1mpleFpsPlayerState (每个玩家 Replicated)                  │
│  Kills, Deaths, Scores, Money, KillStreak, DeathStreak, TeamId      │
│  Delegates: OnScoreChanged, OnMoneyChanged                          │
└───────┬─────────────────────────────────────────────────────────────┘
        ↓ OnScoreChanged / OnMoneyChanged + Replicated Props
┌─────────────────────────────────────────────────────────────────────┐
│              As1mpleFpsPlayerController (每个客户端)                  │
│  HUDWidget, BuyMenuWidget, ChatWidget, PauseMenuWidget              │
│  EnhancedInput (Scoreboard, BuyMenu, Chat, Escape)                  │
│  OnPossess → 绑定 Health/Weapon/Equipment                           │
└───────┬─────────────────────────────────────────────────────────────┘
        ↓ 控制
┌─────────────────────────────────────────────────────────────────────┐
│              As1mpleFpsCharacter (Replicated Pawn)                   │
│  EnhancedInput (Move, Look, Jump, Crouch, Reload, Interact...)     │
│  WeaponInventory[0..2], CurrentWeapon, WeaponIndex                 │
│  ReplicatedHealth, bIsDeadReplicated                                │
│  DamageComponent, StimuliSource (AI 感知源)                         │
│  RPCs: ServerPickUpWeapon, ServerSwitchWeapon, ServerFireWeapon... │
└───────┬─────────────────────────────────────────────────────────────┘
        ↓ 持有
┌─────────────────────────────────────────────────────────────────────┐
│           UTP_WeaponComponent (Replicated Component)                 │
│  WeaponData (DataAsset) → BaseDamage, FireRate, FireMode, Spread...│
│  ReplicatedCurrentAmmo, ReplicatedSpareAmmo                         │
│  StartSingleFire → PerformFire → Spawn Projectile                   │
│  RPCs: ServerFire, MulticastFireEffect                              │
└───────┬─────────────────────────────────────────────────────────────┘
        ↓ 生成
┌─────────────────────────────────────────────────────────────────────┐
│          As1mpleFpsProjectile (Replicated, bNetTemporary)           │
│  OnHit → DamageComponent::ApplyDamage(HitBone, Damage, Penetration) │
│  PlaySound(HitBodySound/HitWallSound) → PlayHitMarker               │
└───────┬─────────────────────────────────────────────────────────────┘
        ↓ 伤害计算
┌─────────────────────────────────────────────────────────────────────┐
│            UDamageComponent (共享伤害组件)                            │
│  ApplyDamage(HitBone, BaseDamage, Penetration, Instigator):         │
│    ZoneDamage = BaseDamage × HitZone Mul                            │
│    FinalDamage = ZoneDamage × (1 - max(0, ArmorReduction - Pen))   │
│  OnDamaged.Broadcast → OnDeath.Broadcast                            │
│  UAISense_Damage::ReportDamageEvent → AI 感知                       │
│  数据: HitZoneData (WeaponDataAsset), EquippedArmors (ArmorData)    │
└─────────────────────────────────────────────────────────────────────┘

                    ┌──────────────────────────┐
                    │    AI 系统 (PvE)          │
                    │  AEnemyAIController       │
                    │  ├── PerceptionComponent  │
                    │  │   (Sight/Hearing/Dmg)  │
                    │  ├── AlertValue → State   │
                    │  ├── Blackboard           │
                    │  ├── Behavior Tree        │
                    │  ├── EnemyFire (LineTrace)│
                    │  └── Squad Coordination   │
                    │                           │
                    │  AEnemyCharacter          │
                    │  ├── DamageComponent      │
                    │  ├── EnemyWeapon          │
                    │  └── die() → GameMode     │
                    │      OnKill + Respawn     │
                    └──────────────────────────┘
```

